# Plan: Описание принципа работы бота в команде /start

## 1. Обновление текста приветственного сообщения

- [x] В `StartCommand.execute()` обновить текст сообщения, описав принцип работы:
  - Пользователь присылает ссылку на YouTube-видео
  - Бот выполняет пересказ содержимого
  - После пересказа можно задавать вопросы по содержимому видео
  - Если прислать новую ссылку — контекст предыдущего видео сбрасывается и начинается обсуждение нового
- [x] Вынести текст сообщения в константу `START_MESSAGE` в `Constants.java` (если его там ещё нет)
- [x] Использовать Markdown-форматирование (жирный текст, списки) для читаемости — если Telegram разметка уже используется в проекте

## 2. Тесты

- [x] `StartCommandTest`:
  - `when_execute_then_welcomeMessageSent`
  - `when_execute_then_messageMentionsNewLinkResetsContext`