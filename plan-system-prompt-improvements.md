# Plan: Улучшение system prompt и ограничение тематики бота

## 1. Запрет раскрытия личности модели

- [x] В system prompt добавь:
  ```
  - не раскрывай, кто ты и какая ты языковая модель; если пользователь спрашивает — уклоняйся от ответа
  ```
  Сам system prompt находится в AiClient.java

## 2. Ограничение тематики диалога содержимым видео

- [x] В системный промпт добавить инструкцию:
  ```
  - отвечай только на вопросы, касающиеся содержимого видео, субтитры которого тебе переданы
  - если пользователь задаёт вопрос, не связанный с этим видео, — вежливо сообщи, что можешь обсудить только содержимое данного видео
  ```

## 3. Устранение фраз про отсутствие инструментов поиска

- [x] В системный промпт добавить инструкцию:
  ```
  - у тебя есть всё необходимое для ответа на вопросы по данному видео; не упоминай отсутствие инструментов поиска или доступа к интернету
  - не ссылайся на внешние источники и не предлагай искать информацию самостоятельно
  ```

## 4. Защита от злоупотребления (несколько ссылок подряд)

- [x] В `BotRetellingHandler.handleRetelling()` добавить проверку: если в сообщении пользователя найдено более одной YouTube-ссылки — ответить предупреждением и обработать только первую ссылку
- [x] Добавить соответствующую константу в `Constants.java`: `MULTIPLE_LINKS_WARNING_MESSAGE`
- [x] Учесть: каждый запрос с новой ссылкой должен списывать доступ (счётчик или оплату)

## 5. Тесты

- [x] `BotRetellingHandlerTest`:
  - `when_handleRetelling_withMultipleLinks_then_onlyFirstProcessed`
  - `when_handleRetelling_withMultipleLinks_then_warningMessageSent`
- [ ] Ручная проверка системного промпта (промпт-тест):
  - Убедиться, что бот не называет себя «языковой моделью» или «ИИ»
  - Убедиться, что бот не отвечает на вопросы не по теме видео
  - Убедиться, что бот не пишет фразы про отсутствие доступа к интернету
