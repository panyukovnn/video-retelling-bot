
# TODO

- Если бот находится в процессе работы с клиентом и клиент напишет еще раз - надо возвращать сообщение, типа: "Я пока занят, дождитесь окончания обработки", реализовать через локи по имени
- Бот продолжает отвечать на вопрос "Кто ты?"

# Youtube video summarizer TG bot

Телеграм-бот для пересказа YouTube-видео с помощью DeepSeek AI

## Как работает

1. Пользователь отправляет ссылку на YouTube-видео
2. Бот загружает субтитры через утилиту yt-subtitles-loader (JAR)
3. Субтитры передаются в DeepSeek API для пересказа
4. Ответ стримится в чат в реальном времени
5. После пересказа пользователь может задавать вопросы по содержанию видео (диалоговый режим с памятью)
6. Новая ссылка на видео сбрасывает контекст и начинает новый диалог

## Модель оплаты

- 1 бесплатный пересказ в день (сброс по московскому времени)
- Пакет из 50 пересказов за 100 звёзд Telegram
- Администраторы — без ограничений

## Стек

- Java 21, Spring Boot, Spring AI
- PostgreSQL + Liquibase
- Telegram Bot API (long-polling через `long-polling-tg-bot-starter`)
- Многопоточная обработка входящих сообщений

## Деплой

Деплой необходимо выполнять на зарубежные сервера, чтобы иметь доступ к YouTube

#### Подготавливаем файлы

- БД развернута на том же сервере в docker-container, конфигурация для подключения к БД хранится в /etc/common-config

1. Создаем docker-compose.yml, помещаем его в /deploy
2. Создаем файл config.yml с секретными конфигурациями для репозитория
3. Выполняем скрипт init-server.sh из папки /deploy
4. Заходим на сервер
5. Выполняем аутентификацию в Github Packages:

`echo YOUR_GITHUB_TOKEN | docker login ghcr.io -u YOUR_USERNAME --password-stdin`

где YOUR_GITHUB_TOKEN — pat с правами read:packages

### Deploy

1. Залить изменения в ветку main
2. Навесить тег
3. Дождаться уведомления в телеграм об успешной сборке
4. Запустить github workflow deploy-tag через UI github
